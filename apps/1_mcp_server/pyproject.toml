[project]
name = "kalshi-mcp-server"
version = "0.1.0"
description = "MCP server that wraps the Kalshi CLI for prediction market access"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "mcp>=1.0.0",
]
